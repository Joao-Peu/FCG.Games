apiVersion: v1
kind: Service
metadata:
  name: fcg-games-svc
  namespace: fcg
  labels:
    app: fcg-games
spec:
  selector:
    app: fcg-games
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
