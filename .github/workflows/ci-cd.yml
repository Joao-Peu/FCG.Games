name: CI/CD
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  ci:
    name: Build & Test1
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore FCG.Games.sln

      - name: Build
        run: dotnet build FCG.Games.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test FCG.Games.sln --configuration Release --no-build --verbosity normal

  cd:
    name: Deploy to AKS
    runs-on: ubuntu-latest
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login to ACR
        run: az acr login --name ${{ secrets.ACR_NAME }}

      - name: Build Docker image
        run: |
          IMAGE=${{ secrets.ACR_NAME }}.azurecr.io/fcg-games
          docker build -t $IMAGE:${{ github.sha }} -t $IMAGE:latest -f Dockerfile .

      - name: Security Scan (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.ACR_NAME }}.azurecr.io/fcg-games:${{ github.sha }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'HIGH,CRITICAL'
          exit-code: '0'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Push Docker image
        run: |
          IMAGE=${{ secrets.ACR_NAME }}.azurecr.io/fcg-games
          docker push $IMAGE:${{ github.sha }}
          docker push $IMAGE:latest

      - name: Set AKS context
        uses: azure/aks-set-context@v3
        with:
          resource-group: ${{ secrets.RESOURCE_GROUP }}
          cluster-name: ${{ secrets.AKS_CLUSTER_NAME }}

      - name: Apply K8s manifests
        run: |
          kubectl apply -f .github/k8s/deployment.yaml
          kubectl apply -f .github/k8s/service.yaml
          kubectl apply -f .github/k8s/ingress.yaml

      - name: Rolling Deploy
        run: |
          kubectl set image deployment/fcg-games \
            fcg-games=${{ secrets.ACR_NAME }}.azurecr.io/fcg-games:${{ github.sha }} \
            -n fcg
          kubectl rollout status deployment/fcg-games -n fcg --timeout=120s
